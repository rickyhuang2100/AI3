---
description: Build the Firebase project by installing dependencies in the functions directory.
---

# Build Project

Follow these steps to prepare the project for development:

1.  Navigate to the `functions` directory:
    ```bash
    cd functions
    ```
2.  Install dependencies:
    ```bash
    npm install
    ```
